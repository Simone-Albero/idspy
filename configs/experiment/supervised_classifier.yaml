# @package _global_
defaults:
  - override /model: tabular_classifier
  - override /pipeline: baseline
  - override /loss: classification
  - override /pred_fn: argmax
  - override /loop: default

experiment_name: supervised_classifier
